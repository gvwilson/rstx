# Session 2026-03-31T02:00:00 UTC

## Prompt

Read `../rsdtx.txt` and execute the instructions in it.

## Instructions in `../rsdtx.txt`

1. Convert math notation in `forma-*` assessment divs to KaTeX.
2. Identify glossary terms in the 7 lessons; add `[%g key "text" %]` shortcodes and create `glossary.md`.
3. Add glossary entry format documentation to `CLAUDE.md`.

## Actions Taken

### Task 1: KaTeX in formative assessments

Edited all 7 lesson files to replace Unicode math symbols with KaTeX `$...$` notation in every `forma-*` div. Changes included:

- `diffusion/index.md`: `DX²` → `` `DX**2` ``; `∂c/∂x = 0` → `$\partial c/\partial x = 0$`; `c = 0` → `$c = 0$`; `σ²(t) = σ₀² + 2Dt` → `$\sigma^2(t) = \sigma_0^2 + 2Dt$` etc.; Python constants wrapped in backticks.
- `lotka/index.md`: `x* = d/c` → `$x^* = d/c$`; `y* = a/b` → `$y^* = a/b$`; `H(x,y) = cx − d·ln x + ...` → KaTeX; `ln x → −∞` → `$\ln x \to -\infty$`; matching table terms `ax`, `bxy`, `cxy`, `dy` → `$ax$` etc.
- `align/index.md`: `GAP_PENALTY`, `MISMATCH_PENALTY`, `MATCH_SCORE` wrapped in backticks.
- `phylo/index.md`: `2×8 − 5` → `$2 \times 8 - 5$`; `2N − 3` → `$2N - 3$`; `O(N²)` → `$O(N^2)$`; `Σ_k D(...)` → `$\sum_k D(\text{...})$`; `ℓ_i = ...` → full KaTeX formula; `ℓ_Human` → `$\ell_\text{Human}$`.
- `massspec/index.md`: `σ_noise` → `$\sigma_\text{noise}$`; `√5` → `$\sqrt{5}$`; `1/√w` → `$1/\sqrt{w}$`; `≈ 1.8σ` / `≈ 4.5σ` → KaTeX; parameter names backticked; `min_distance` backticked.
- `cellseg/index.md`: `σ = 6`, `σ_s = 2`, `B · σ² / (σ² + σ_s²)` → KaTeX; `MIN_CELL_SIZE` backticked; `4σ = 8` → `$4\sigma = 8$`; `≈ 24σ` → `$\approx 24\sigma$`.
- `radvel/index.md`: `v(t) = K · sin(...)` → full KaTeX; `$\varphi$` and `$v_\text{sys}$`; matching table with `$\sqrt{2}$`, `$K / \sqrt{2}$`; `σ_K / K` → `$\sigma_K / K$`; `2σ` → `$2\sigma$`; flashcard `v_sys` → `$v_\text{sys}$`, `M_p sin i` → `$M_p \sin i$`.

### Task 2: Glossary

Created `glossary.md` with 22 entries covering:
`binary_mask`, `connected_component`, `covariance_matrix`, `diffusion_equation`, `distance_matrix`, `dynamic_programming`, `first_integral`, `gaussian_filter`, `ghost_cell`, `local_alignment`, `lotka_volterra`, `moving_average`, `neighbor_joining`, `non_maximum_suppression`, `phase_portrait`, `phylogenetic_tree`, `q_matrix`, `radial_velocity`, `semi_amplitude`, `smith_waterman`, `truncation_error`, `von_neumann_stability`.

Inserted `[%g key "text" %]` shortcodes at the first prose occurrence of each term in the appropriate lesson file.

### Task 3: CLAUDE.md

Added a "Glossary" subsection to `CLAUDE.md` documenting:
- The `<span id="key">Term</span>\n:   definition` format for `glossary.md`.
- The `[%g key "display text" %]` shortcode syntax for referencing glossary terms in lessons.
