# Log 2026-03-30T06:00:00 UTC

## Prompts

1. Do the cellseg lesson, including what the detector finds on pure noise.

## Actions

- Read cellseg/index.md stub and ~/.claude/principles.md.
- Created cellseg/generate_image.py: make_image() (Gaussian blobs, non-overlapping placement,
  additive noise, clipped to 0), make_pure_noise(); constants IMAGE_SIZE=128, N_CELLS=8,
  CELL_SIGMA=6, NOISE_SCALE=0.15, MIN_SEPARATION=24 (derived from threshold condition).
- Created cellseg/cellseg.py: smooth() (scipy.ndimage.gaussian_filter), segment()
  (threshold → label → size filter → re-number), cell_sizes(), plot_segmentation()
  (two-panel matplotlib: greyscale raw + coloured label overlay).
- Created cellseg/test_cellseg.py: 7 tests.
- All 7 tests pass on first run.
- Fixed noise demo: original min_size filter removed most noise blobs, leaving only 1.
  Changed noise demo to min_size=1 to show the full extent of false-positive behaviour
  (4 blobs visible at threshold=0.05).
- Generated cellseg.png (8/8 cells found, areas 143–150 px²) and cellseg_noise.png
  (4 false blobs at threshold 0.05, 0 at threshold 0.50).
- Wrote cellseg/index.md: Gaussian cell model (KaTeX), smoothing convolution formula and
  peak attenuation, threshold derivation (24σ), noise demo section, testing section, 4 exercises.
