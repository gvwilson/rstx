# Log 2026-03-30T05:00:00 UTC

## Prompts

1. Start a new log file and implement the massspec lesson, including the synthetic data
   generator, with an example of what the detector finds on pure noise.

## Actions

- Read massspec/index.md stub and ~/.claude/principles.md.
- Created massspec/generate_spectrum.py: make_spectrum() (Gaussian peaks + noise),
  make_pure_noise() (noise only); constants MZ_MIN, MZ_MAX, N_POINTS, PEAKS, DEFAULT_NOISE.
- Created massspec/massspec.py: smooth() (moving average via np.convolve mode='same'),
  detect_peaks() (local maxima + non-maximum suppression), plot_spectrum() (layered Altair);
  constants SMOOTH_WINDOW=5, THRESHOLD=0.10, MIN_DISTANCE=15.
- Created massspec/test_massspec.py: 7 tests.
- Fixed test_min_distance_suppresses_duplicates: original peaks (sigma=1, sep=2 Da) merged
  into one maximum before detection; fixed to sigma=2, sep=10 Da on 101-point grid.
- Generated massspec.svg and massspec_noise.svg by running massspec.py.
  Pure noise at threshold=0.04 produces 9 false peaks; at threshold=0.10 produces 0.
- Rewrote massspec/index.md: KaTeX Gaussian and moving-average formulas, both figures,
  threshold justification (4.5σ operating point), testing section, 4 exercises.
- All 7 tests pass.
