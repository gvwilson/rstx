# Session 2026-04-10T00:00:00Z

## Prompts

- Write the interp, learning, sdt, and agreement lessons.

## Actions

### interp (Spatial Interpolation of Climate Data)

- Created `interp/generate_interp.py`: `make_stations` places 30 stations
  uniformly at random in [0,1]^2 and samples their values from
  T(x,y) = sin(3*pi*x)*cos(2*pi*y); `make_true_grid` returns the reference
  field on a 40x40 grid.
- Created `interp/interp.py`: `idw` implements inverse-distance weighting
  (p=2) using numpy broadcasting to compute all (m,n) distances at once;
  `interpolate_grid` wraps IDW for a regular grid; `plot_comparison` saves a
  side-by-side matplotlib heatmap of the true and interpolated fields.
- Created `interp/test_interp.py`: 6 tests covering station count, exact
  recovery at a station location, grid shape, convex-combination value bounds,
  weight-distance ordering, and LOO-CV MAE below 0.3.
- Created `interp/index.md`: full lesson with multiple-choice, numeric entry
  (IDW formula), and ordering assessments; code inclusions; figure; LOO-CV
  section; testing section; flashcard; four exercises.
- Generated `interp/interp-fields.svg`.
- Added glossary entries: `cross_validation_loo`, `inverse_distance_weighting`,
  `spatial_interpolation`.

### learning (Fitting a Learning Curve to Experimental Data)

- Created `learning/generate_learning.py`: `make_trials` generates 80 trials
  with RT = 500 * n^(-0.3) + Gaussian(0, 20 ms); values below 1 ms clamped.
- Created `learning/learning.py`: `fit_power_law` calls
  `scipy.optimize.curve_fit` and returns (a, b, b_ci) where the 95% CI uses
  z=1.96 and SE = sqrt(pcov[1,1]); `plot_fit` saves an altair scatter + line
  chart.
- Created `learning/test_learning.py`: 6 tests covering trial count, all-RT
  positive, trial number sequence, exponent within 0.10 of true, CI bracketing
  true, and predicted RT decreasing.
- Created `learning/index.md`: full lesson with multiple-choice, numeric entry
  (power-law evaluation), and ordering assessments; math for the CI derivation;
  code inclusions; figure; testing section; flashcard; four exercises.
- Generated `learning/learning-curve.svg`.
- Added glossary entries: `confidence_interval_parameter`, `learning_curve`,
  `power_law`.

### sdt (Signal Detection Theory Analysis)

- Created `sdt/sdt.py`: `d_prime` and `criterion` implement the inverse-normal
  formulas; `roc_curve` sweeps the criterion from CRITERION_MAX=4 to
  CRITERION_MIN=-4 using scipy.stats.norm; `plot_roc` saves an altair chart
  with curve, diagonal, and operating point.
- Created `sdt/test_sdt.py`: 8 tests covering d'=0 at HR=FAR, sign of d'
  for HR>FAR and HR<FAR, known d'=2 from boundary probabilities, c=0 for
  unbiased observer, ROC near (0,0) at high criterion, ROC near (1,1) at low
  criterion, and ROC monotone.
- Created `sdt/index.md`: full lesson with equal-variance Gaussian model,
  d'/criterion derivation, multiple-choice, numeric entry (d' = 2.0), ROC
  construction, and ordering assessment; code inclusions; figure; testing
  section; flashcard; four exercises.
- Generated `sdt/sdt-roc.svg`.
- Added glossary entries: `criterion_sdt`, `d_prime`, `false_alarm_rate`,
  `hit_rate`, `roc_curve`, `signal_detection_theory`.

### agreement (Inter-Rater Agreement)

- Created `agreement/generate_agreement.py`: `make_ratings` generates 100
  paired label vectors (3 categories); with probability agree_prob both raters
  draw the same label, otherwise they draw independently; expected kappa equals
  agree_prob under uniform marginals.
- Created `agreement/agreement.py`: `contingency_table` builds the K x K count
  matrix; `cohen_kappa` computes kappa and SE = sqrt(P_o*(1-P_o)/(N*(1-P_e)^2));
  `plot_kappa_scenarios` saves an altair bar chart across five agreement levels.
- Created `agreement/test_agreement.py`: 6 tests covering table shape, table
  sum, perfect-agreement kappa=1, chance-agreement kappa near 0, SE positive
  and finite, and kappa within 0.15 of agree_prob.
- Created `agreement/index.md`: full lesson with chance-agreement derivation,
  kappa formula, expected-kappa-equals-agree_prob proof, SE formula, multiple-
  choice, numeric entry (kappa=0.5 from symmetric 3x3 table), code inclusions;
  figure; testing section; flashcard; four exercises.
- Generated `agreement/agreement-kappa.svg`.
- Added glossary entries: `cohens_kappa`, `contingency_table_raters`,
  `inter_rater_agreement`.
