# Session 2026-04-08T20:43:05Z

## Prompt

Write the wordshift and stemma lessons.

## Actions

### wordshift lesson

- Created `wordshift/generate_wordshift.py`: generates synthetic decade-level word
  count data (decades 1850–1950) with three target words ("telegraph", "candle",
  "steam") having injected linear frequency trends (+0.002, -0.002, 0.000 per decade)
  and 97 background words with stable frequencies; uses multinomial sampling with
  5000 tokens per decade and seed 7493418.
- Created `wordshift/wordshift.py`: normalizes raw counts by total tokens per decade,
  estimates OLS trend slopes using the closed-form formula, and plots frequency
  trajectories with Vega-Altair.
- Created `wordshift/test_wordshift.py`: seven tests covering normalization correctness,
  slope recovery for flat/known-slope/single-point series, and slope sign and magnitude
  for all three target words.
- Replaced stub `wordshift/index.md` with full lesson covering normalized frequency,
  linear trend slope, the generator, three formative assessments (multiple-choice,
  two numeric-entry), flashcard, and four exercises.
- Generated `wordshift/wordshift-trajectory.svg`.

### stemma lesson

- Created `stemma/generate_stemma.py`: simulates a known four-manuscript copying tree
  (archetype -> alpha -> {A, B}; archetype -> beta -> {C, D}) by propagating random
  binary mutations (prob 0.05) with seed 7493418.
- Created `stemma/stemma.py`: defines exact branch lengths, builds a tree-additive
  distance matrix, runs the neighbor-joining algorithm (same implementation as the
  phylo lesson), and draws the unrooted tree with networkx/matplotlib.
- Created `stemma/test_stemma.py`: nine tests covering Hamming distance edge cases,
  distance matrix symmetry and within-/cross-family ordering, edge count, topology
  recovery, exact branch-length recovery to 1e-10, and generator topology check.
  Fixed a logic error in `test_hamming_half` where all-flipped sequences gave
  distance 1.0 instead of 0.5.
- Replaced stub `stemma/index.md` with full lesson covering variant loci, Hamming
  distance, the neighbor-joining algorithm and worked example Q-matrix table, three
  formative assessments (multiple-choice, two numeric-entry), flashcard, and four
  exercises.
- Generated `stemma/stemma-tree.svg`.

### Glossary additions

Added to `glossary/index.md`:
- `diachronic_analysis` (D section)
- `hamming_distance` (H section)
- `normalized_frequency` (N section)
- `stemma` (S section)
- `stemma_reconstruction` (S section)
- `variant_locus` (V section)
- `word_shift` (W section)
